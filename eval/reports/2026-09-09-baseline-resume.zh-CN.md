# 全量基线续跑记录（非最终成绩）

记录时间：2026-09-09 17:37，Asia/Shanghai。本报告只记录断点完整性与运行限制，不据部分结果选择优化方向。

最新续执行（2026-09-11）：用户已明确免跑最后九题，后续固定分析 2,052 份完整六组结果，不再等待补齐。见[已接受样本基线](2026-09-11-personamem-effect-baseline.zh-CN.md)与[首项固化局部修复](2026-09-11-consolidation-target-marker.zh-CN.md)。原始全量 verifier 仍拒绝缺九题；下文保留当时的运行与待决记录，不代表现在仍需用户再次决定这九题。

## 范围与冻结配置

- OmniMemEval：固定上游 `0b1ea8d28aa2d3e03ac4a6aee17b3006a131da7d`，完整 5,000 题；沿用上游 prompt、选项、计分和逐题 checkpoint。
- PersonaMem-v2：完整 validation 2,061 题、735 个 persona，六组保持 `none / full_history / oracle_episode / semantic_top40 / current_core / learned_core`。
- 两条路径的回答模型均为 `MiniMax-M2.5`。本次续跑没有改变 Core、Worker、回答 prompt、选题或记忆预算。
- PersonaMem manifest 中六份 harness 源码的 SHA-256 与当前文件逐一相同；`persona_workers=8` 保持不变。后台实例恢复另记在该实验的 `runtime-replicas.json`，不覆盖 manifest。

## 断点审计

| 检查 | 当前证据 |
|---|---|
| PersonaMem 完整六组题目 | 886 / 2,061；题目与 persona 身份匹配 manifest |
| PersonaMem Oracle 与漏斗 | 通过现有汇总器的六组互斥、Oracle 隔离和漏斗一致性检查 |
| PersonaMem 冻结记忆 | 270 份快照逐份与 `DatabaseProbe.memory_state` 的当前数据库结果相等 |
| PersonaMem 完成证明 | 262 份 persona 完成证明；快照 hash、已完成题目集合、记忆未变声明均核对 |
| PersonaMem 原始经历与后台队列 | 35,052 个 Episode；待巩固 Job 为 0，待同步索引操作为 0 |
| PersonaMem 模型请求缓存 | 4,681 份；恢复仍按原有完整请求匹配，不按答案正误复用 |
| Omni 检索 | 5,000 条非空 context，预生成 `reflect_answer` 为 0 |
| Omni 回答输入 | 已保存的 561 题均逐字匹配上游 `PM_ANSWER_PROMPT`、原检索 context 与原选项 |

Omni 的 561 份 checkpoint 中有一条空答案：`pm_exper_user_154_memory-core-minimax-minilm-v1`。上游记录为请求成功、回答错误。本次保留其原始计分，不删除、不选择性重测。上游 checkpoint 未保留完整响应及 `finish_reason`，因此不能断言它是截断、模型空输出还是答案提取结果；最终报告须单列无效答案，不能把“请求状态成功”写成“每条都有有效答案”。

## 限流与调度

PersonaMem 在返回 Token Plan `429 / 2062` 后退出，断点和请求缓存仍在。随后最小请求返回 HTTP 200、`OK`，用时 1.29 秒；查询时窗口额度剩余 25%、周额度剩余 71%。这说明当前 Key 有效，查询未显示窗口额度耗尽；现象符合短时限流，但未测得账户的具体 RPM/TPM 上限。

Token Plan 的短时速率限制与 5 小时固定窗口、周窗口额度是不同约束；官方也说明高峰期可能动态收紧。[官方 FAQ](https://platform.minimaxi.com/docs/token-plan/faq)

当前先让 Omni 的原定 4 并发单独运行，暂不恢复 PersonaMem，避免两套评测同时消耗账户速率额度。额度耗尽时应暂停请求并保留断点，不能把 429 当作模型答错；恢复仍使用同一实验版本和原配置，不通过降低样本量完成目标。

17:49 补记：API 随后明确返回用量上限 `429 / 2056`，额度接口报告窗口剩余 0%、周剩余 68%，窗口结束时间为当日 20:00。Omni 已保存 794 个成功请求 checkpoint，其中仍有上述 1 个空答案。已读取的暂挂前日志至少可见 23 个不同题目的请求失败（5 个 `2062`、18 个 `2056`），没有被保存成错误答案。工具输出发生过截断，完整失败数须以上游最终 `failed_users` 与缺题集合核对，不能把 23 写死为补跑题数。

上游 token tracker 只在回答进程结束时落盘，因此本次没有终止回答进程，而是在 17:48:37 将原进程暂挂，并验证其状态为 `T`。额度观察进程按同一 PID、父进程和启动时间核对身份；实际窗口及周额度重新可用后，恢复原进程，继续保留其内存统计。PersonaMem 暂不恢复。若 Omni 因已记录的配额失败而在本轮结束时报错，先归档本轮 `token_usage_answer.json`，再以同一版本补齐缺题并合计各轮统计，不能把最后一次续跑的 token 数冒充完整 5,000 题成本。

18:55 补记：观察进程于 18:49:13 查询到窗口及周额度均为 100%、状态正常，已自动恢复原回答进程，不依赖此前预计的 20:00 时间。当前 955 份 checkpoint 的实际输入仍逐字匹配上游 prompt、检索正文和选项，空答案仍为 1 条；六份冻结 harness 文件的校验值未变。等待期间曾出现 DNS 与 TLS 校验失败，观察进程没有据此恢复，也未关闭证书校验。

暂挂时已经开始的 4 个请求，记录的 `response_duration_ms` 含约一小时等待：题目 ID 尾号为 `333 / 334 / 2637 / 4941`。保留这些原始记录，不为获得漂亮时延而删除或改写；最终准确率仍按上游规则计算，但这次原始墙钟时延不能直接用于框架性能排名，必须标注配额暂停。其余完整结果仍待全量运行结束后核验。

19:17 补记：外层恢复器已换为 `.cache/omni-eval-supervisor-v2.zsh`，回答进程没有重启。修复的是原恢复器要求“退出后必须亲自观察到额度归零”的缺口：只有上游记录的失败题恰好等于全部缺题，且全为 `429 / 2056` 或 `429 / 2062` 时，才会在额度可用后运行原续跑命令；未知错误、重复失败项、题目不对应或未解释的缺题均停止。已有错答和空答案仍由上游 checkpoint 保留，不选择性重测。

每次补跑前，当前 `token_usage_answer.json` 会按内容 hash 保存到该实验的 `answer-passes/`；无法保留时停止，避免上游下一轮覆盖这份计数。最终成本报告仍须合计这些分段与最后一轮统计。19:36 补充验证：已有存档仅在字节一致时复用；补跑异常退出且没有原子发布新状态报告时停止，不能沿用旧的限流报告授权再次重试。恢复控制在旧版本上重现失败，新版本通过 11 项不访问真实 API 的回归测试；仅修改本地运行辅助文件，未改变冻结 harness、Core、Worker 或上游源码。

19:58 补记：原回答进程仍在推进，新增日志出现至少一条非限流失败：`pm_exper_user_1181_memory-core-minimax-minilm-v1` 返回 HTTP `422`、`output new_sensitive (1027)`，请求标识为 `06f07c6d89d15b86b945e02b3af4aada`。该题的检索状态为 success、context 为 15,263 UTF-8 字节，未使用预生成答案；API 未提供可计分的回答。供应商将 1027 列为输出内容问题，具体被拦截的输出不可见，不能据此断言是哪段内容触发。[官方错误码](https://platform.minimax.io/docs/api-reference/errorcode)

上游 `llm_client.py` 将 HTTP 422 判为不可重试；`pm_responses.py` 在当前 `skip_failed_answer=false` 下保留失败，并继续其余题，最后才发布完整 `failed_users`。现有恢复器只接受已核对的限流失败，因此本轮结束后若仍含此错误，会停止而不是自动绕过。已核实 `pm_metric.py` 排除所有非 success 记录：开启“跳过失败题”会缩小准确率分母，不能将其宣称为完整 5,000 题横评。本次没有修改题目、prompt、跳过开关、计分或恢复器；等待本轮全部请求结束后，以完整错误清单确定剩余处理路径。

20:15 补记：逐份核对当前 3,017 个回答 checkpoint，身份、题目、gold、选项和实际 `model_input` 均与固定检索产物及上游 `PM_ANSWER_PROMPT` 一致，未使用 `reflect_answer`。检查中发现此前口头将所有空 `answer` 字段概括为“错答”不准确：当前 10 条处理后文本为空的记录中，9 条原生 `is_correct=false`，1 条（题目尾号 `2709`）原生 `is_correct=true`。

原因在上游 `extract_choice_answer` 的双路径计分：最终标签内提取不到选项时，还会从未裁剪的回复中查找唯一选项，但持久化的 `answer` 仍可为空。直接执行固定上游函数，以 `The answer is (a). <final_answer></final_answer>` 为不访问模型的反例，可复现 `(True, '')`。这只是说明该状态符合上游机制，不代表恢复了该题不可见的原始回复。最初检查中“空字段必为 false”的断言因此不成立；去掉这一错误假设后，全部输入一致性检查通过。没有改写任何分数、答案或 checkpoint。最终应分列“处理后字段为空、其中原生判对/判错”，不能把它等同于模型原始空输出或擅自统一判错。

21:38 补记：系统电源日志确认本机于 `20:37:14 +0800` 因 `Clamshell Sleep` 休眠，期间只有短暂 DarkWake，至 `21:33:26 +0800` 才正常唤醒。原回答进程 PID 69700、父进程 69653 均仍在，唤醒后断点已推进至 3,687 题；没有因观察时间跳变而重启。额度观察器在此期间只新增一条 DNS 解析失败日志，没有新的配额暂停/恢复记录。

题目尾号 `1661 / 3965 / 1654 / 3963` 的原生 `response_duration_ms` 为约 339–344 万毫秒，与休眠窗口重合，包含约 56–57 分钟的等待。连同此前配额暂停的 4 条，目前共有 8 条超过 10 分钟的耗时记录。保留全部原始时延与计分；本次墙钟时延受到配额暂停和本机休眠共同影响，不能直接作为模型/框架速度排名。没有修改系统电源设置或阻止用户主动合盖。

22:46 补记：Omni 首轮已终止，完整状态为 4,971 条成功、29 条失败；28 条限流与 1 条内容拦截恰好覆盖全部缺题。旧恢复器按预期停止。归档首轮 usage 与状态后，本地 `.cache/omni_rate_only.py` 经 6 项无 API 测试和实际预检，只补跑了 28 条限流题，全部成功，未调用内容拦截题。补跑后 4,999 条实际输入逐字匹配上游输入，原 4,971 条记录的规范化 hash 未变，拦截错误记录未变。

两轮原始 usage 已归档，合计 4,999 次成功请求、17,265,702 个 API 返回的 token；合并统计另存 `token_usage_answer_total.json`，最后一轮原始文件仍保留。上游原生计分为 1,942 / 4,999 = 38.8478%，仍明确报告 1 条失败、0 条跳过。完整 5,000 分母下的拒答处理已通过非阻塞问题请求用户确认，尚未视为已批准。详情见 `2026-09-09-omni-minimax-baseline.zh-CN.md`。

PersonaMem 六组已按原 manifest 与同一输出目录恢复，当前评测进程 PID 84086、终端 session 5926；原 8 persona 并发、5 个 Core 实例和 6 份 harness 文件校验值保持不变。已从 886 推进到 905 个逐题 checkpoint，仍非全量结果。旧 Omni PID 69653/69700、额度观察器 71046、恢复器 75620 及本次限流补跑均已结束，不能再把它们当作活跃任务轮询。

23:43 补记：PersonaMem 原评测进程 PID 84086、session 5926 已以 exit 1 结束，终端明确返回 MiniMax `422 / output new_sensitive (1027)`，请求标识为 `06f0ad7540da46edea7a4994b3a96320`。这次不是仍在运行的等待，也不是额度耗尽。保留了 982 份完整六组结果、5,183 份成功请求缓存与 294 份 persona 完成证明；没有生成全量 summary。

停机后核验：982 题的六组完整性、题目身份、Oracle 标记及漏斗正确性对应均通过；297 份已冻结记忆快照逐份与当前数据库相等。此前后台曾出现索引与巩固超时，随后待巩固 Job 和待同步索引操作均降到 0；没有修改索引算法、Core 或 Worker。原 6 份 harness 源码 hash 仍与 manifest 一致。

原回答路径只缓存成功请求，没有持久化失败请求的题号和分组，因此不能精确归因这一次 422。中断时有 3 个已冻结但未完成的 persona：`3 / 335 / 756`，首个未完成题分别为 `row-00364 / row-00361 / row-00362`。这表示归因尚不明确，**不表示三者都被内容拦截**。这 3 个 persona 合计 8 道未完成题暂留，不重新请求；其余 1,071 道缺题继续执行，完整 2,061 题目标不变。

本地 `.cache/personamem_resume_others.py` 是运行层隔离续跑器：保留原 `run_personas_bounded` 的 8 并发和原 persona 内部执行，暂留上述 3 个 persona；后续只有明确的供应商 `422 / 1027` 才记录原始请求正文、对应题号、可用的请求标识与 hash，并暂留该 persona。请求无法唯一对应题目、未知 422、401、429 或记录落盘失败仍抛错；不会自动重试内容拦截，不写伪造答案，也不修改计分。原全量 verifier 保持生效，实际验证仍拒绝缺题产物。

恢复器先在无操作版本上复现失败，再通过 11 项不访问 API 的测试；包括不再请求待处理 persona、其他 persona 能继续、失败正文不含鉴权 header、未知错误不被吞掉，以及冻结源码漂移时拒绝运行。其运行状态保存在同一输出目录的 `operational-deferrals.json`，记录原 manifest SHA-256 `8612287421f11e000e1220d5732c78c6c81eb713717b7150742615bf4fbefbd4` 和恢复器 SHA-256 `def319170bc09c0829dd67603e78bf0d9a0fcf50f874ed2722b0f26a0223139c`。该恢复策略须随结果披露，不能只凭原源码 hash 声称调度路径未发生变化。

新的续跑进程为 PID 85757、session 91780，已确认存活并逐项输出上述 3 个 persona 的 `no_request`。启动前 5 个 Core 实例均返回 `/health/ready` 200，窗口额度剩余 36%、周额度剩余 90%。模型、原请求输入、选项、预算、Core/Worker/index 配置及现有成功缓存保持不变。供应商拦截是否计为未答并保留全量分母，已统一针对 Omni 与六组请求用户确认；提问工具的 `accepted` 仅表示问题已展示，不代表用户批准。目前未改变分母或评分。

2026-09-10 00:12 补记：续跑中新增 `persona 286 / row-01756` 的 `422 / 1027`。运行恢复器已保存其真实请求正文并继续其他 persona；请求模型仍为 `MiniMax-M2.5`，`max_tokens=8192`、`temperature=0`。请求正文 SHA-256 为 `94a3ed0cdc9bf3f2febc11bf3673ef388d9fe4e5555b1e2bc7ca1bc9e1437412`；记录里的 `request_id` 为 null，只能说明恢复器未取得该字段，不能据此断言供应商没有分配标识，更不能补造。该题没有成功回答，不计成成功或选择性重试。暂留范围现为 `3 / 335 / 756 / 286` 四个 persona、9 道未完成题，其中新增 persona 仅余这一题；并非四个 persona 都有已证实的内容拦截。进程 PID 85757、session 91780 在此次拦截后仍存活，其他题继续落盘。

2026-09-10 04:14 补记：03:59:58 核验时，续跑进程 PID 85757 已退出，session 91780 明确返回 exit 1、MiniMax `429 / 2056`，请求标识为 `06f0eda29d770fd1dd85245e2caa99ff`。额度接口报告当前窗口剩余 0%、周剩余 79%，窗口结束时间为北京时间 05:00。04:10 再以当前 `env/prod/chorai_agent.env` 的 Key 查询套餐成功，最小 `MiniMax-M2.5` 请求也明确返回同一用量上限错误；这不是鉴权失败，也不是新的内容拦截。不能把套餐查询成功写成当前模型可生成。

本次断点核验：1,373 题的六组完整性、题目身份、Oracle 和漏斗一致性通过；426 份冻结记忆逐份与当前数据库相等，且对应 owner 的待同步索引为 0；421 份 persona 完成证明的快照 hash、完整题目集合和记忆未变声明通过。成功请求缓存为 7,209 份。六份冻结 harness 源码、manifest、运行恢复器和 Core 二进制的 hash 均保持原值。全量 verifier 仍以缺题拒绝通过，未生成最终成绩。

运行恢复器预检仍暂留 `3 / 335 / 756 / 286` 四个 persona 的 9 题，另有 679 道缺题可按原配置继续；没有追加暂留、改变分母或重试已知拦截。当前数据库有 53,448 个 Episode、7 个待巩固 Job（最高 attempts=7）、0 个待同步索引操作。待处理任务保持原有窗口与重试机制，不以恢复为理由改写已冻结的回答阶段记忆。

04:10:20 已对本次 PersonaMem 专用的五个 Core 实例及 Worker 发送 `SIGSTOP`，逐一验证为 `T` 状态，避免零额度下继续重试。所有进程父 PID 为 65561，工作目录均为独立 `memory-core`；Core PID / 启动时间为 `68017 / 09-09 16:36:48`、`69487 / 17:10:57`、`69496 / 17:10:58`、`69503 / 17:11:00`、`69510 / 17:11:01`，命令均为 `./bin/memoryd`；Worker 为 `67576 / 09-09 16:23:46`。MySQL 与索引 PID 67944 保持运行，未操作 Chorai 主线或 Omni 服务。暂停可能影响未完成后台请求的墙钟耗时，须保留原始记录并披露，不能用于速度排名。

只读额度等待器 session 25775 每 45 秒查询一次，不发送生成请求、不自动恢复服务。DNS、TLS 或缺失额度字段不视为恢复；只有接口同时报告窗口与周额度为正、状态正常，才允许下一步按已记录的 PID、父进程和启动时间核验身份后 `SIGCONT`，检查 `/health/ready`，再以同一恢复器、manifest、模型、预算、并发和输出目录续跑。不能仅根据预计 05:00 或已经失效的 evaluator session 决定恢复。截至 04:13:21，额度仍为 0%，尚未恢复评测。

2026-09-10 05:04 补记：只读等待器在 05:00:05 实际查询到窗口剩余 100%、周剩余 79%，两项状态均正常，随后以 exit 0 结束；其 session 25775 不再是活跃句柄。先核对原六个服务的 PID、父进程、启动时间、工作目录与 Core 二进制 hash，再于 05:01:25 对相同进程发送 `SIGCONT`。五个 Core 的 `/health/ready` 均返回 200，没有新建替代实例或更换配置。

服务恢复后再次逐份核验：1,373 题六组结果、426 份数据库冻结记忆和 421 份完成证明均通过，成功请求缓存仍有 7,209 份；全量 verifier 仍因缺题拒绝通过。恢复器预检确认 679 题可续跑、原 9 题仍暂留。新回答进程 PID 99503 于 05:03:06 启动，终端 session 71955，使用同一恢复器、输出目录、manifest、MiniMax-M2.5、8 persona 并发、预算和所有原参数；日志逐一确认四个暂留 persona 为 `no_request`。

05:03 后 Worker usage 与 window 日志继续更新；抽查时待巩固和待索引队列均为 0。暂停仅作用于运行调度，原有请求、窗口、缓存、答案及计分未改写。05:04 检查时新进程仍存活，完整题目数暂为 1,373；恢复启动不是新增题目已完成的证明，后续仍须核对真实落盘结果和全量漏斗。

05:06 补记：新进程已输出 `persona 73 / row-01249 / row-02010` 的完整六组结果，逐题文件数增至 1,375，进程 PID 99503 仍存活。恢复已产生新的实际 checkpoint，仍不据此宣称全量通过或选择 Core 优化方向。

2026-09-10 06:08 只读运行核查：逐题结果在 1,430 附近出现较长等待。Core 日志确认 MemoryIndex projection 的 `DeadlineExceeded` 重试，以及部分自动巩固的数据库加载或 Worker RPC deadline 错误；不能仅凭进程存活就称所有请求健康，也不能把这些超时当作模型答错。

最近 300 条索引完成日志中，Search 170 条，p50 约 1,723 ms、p95 约 3,502 ms；Upsert 130 条，p50 约 1,472 ms、p95 约 3,199 ms。该样本有 35 组相同 owner/ref/text hash 的重复 Upsert，涉及 75 条记录。索引实现用一个锁串行执行编码及 Chroma 操作，日志从 RPC handler 开始计时，不含进入 handler 前的线程池排队；这解释了为何已完成日志的耗时不能直接代表客户端总等待。首批 300 条日志的对应 p50 仅约 13 ms / 30 ms，但两个样本负载和数据量不同，不能据此声称完成了控制变量性能实验，或把增长全部归因于某一算法。

代码中 Core 的默认索引调用期限为 5 秒，巩固整个窗口的期限为 2 分钟；本次没有从运行进程读取敏感环境，也没有把源码默认值冒充已核实的实际环境覆盖值。数据库抽查仅见当前短查询和空闲连接，容器 CPU 约 10.7%，该时点未见长时间执行的数据库查询。这些证据支持索引路径吞吐压力及超时重复工作，尚不足以排除所有其他运行因素。

并未完全停止推进：06:00 至 06:07 的只读检查中，Episode 数由 55,970 增至 56,171，待巩固任务由 8 降至 6，当前最高 attempts 由 5 降至 2；Worker usage/window 日志更新至 06:06:57，索引日志更新至 06:07:36。不同检查时的任务集合会变化，不能把最高 attempts 的下降说成同一任务计数回退。继续现有冻结基线，未改超时、并发、索引实现、模型、prompt 或计分；这份运行观察不替代最终准确率漏斗和唯一效果瓶颈的选择。

2026-09-10 08:13 补记：评测进程 PID 99503、session 71955 已明确以 exit 1 结束，停在 1,498 份完整六组结果。异常是 `effect preparation incomplete: {'episodes': 140, 'jobs': 5, 'pending_jobs': 1, 'pending_index_operations': 0}`，即历史准备超过原定 1,800 秒等待上限，不是配额错误或内容拦截。后台查询中，`persona-912` 的第 5 个窗口含 32 个 Episode ref，同一 Job 已尝试 12 次、存在约 31 分钟；另有 6 个未完成 Job。Core 日志持续出现索引 RPC deadline，以及巩固加载 Episode、索引关联和 receipt 时的 deadline。

进一步核对源码：巩固会逐条查询窗口中不重复的 Situation，索引失败只跳过当条查询；整个准备、Worker 与提交共用默认 2 分钟期限。索引 handler 内用一个锁串行编码与 Chroma 操作，不能仅凭后续数据库加载报错就归因为慢 SQL。数据库检查仍仅见短查询与空闲连接。本次做了一次不返回候选、不生成文本、不写记忆的空查询探测（同一 scope，`text='' / limit=0`）：RPC 往返约 2,301 ms，handler 日志约 2,299 ms；该样本没有观察到显著的 handler 前排队，因此不能据此声称已证明线程池排队是主因。未修改超时、并发、检索算法或 Core/Worker 代码。

评测退出后，原有后台服务继续处理同一批窗口；08:11:12 的快照为 58,946 个 Episode、0 个待巩固 Job、0 个待同步索引操作。没有重启后台服务或手工改写 Job。续跑前重新验证：1,498 题的六组结果一致性通过，472 份冻结记忆与当前数据库相等，468 份完成证明通过，成功请求缓存为 7,818 份；全量 verifier 仍因缺题拒绝通过。原 Core 二进制、manifest、恢复器及六份 harness 源码 hash 均未变。五个 Core 的 `/health/ready` 均为 200，窗口剩余 59%、周剩余 75%。

08:13:18 使用同一恢复器、输出目录和全部原参数启动新评测进程 PID 7040、session 70054；仍有 554 道可续跑缺题，原四个 persona 的 9 题继续暂留，没有扩大暂留范围、改写失败为答案或缩小分母。本次是准备超时后的断点恢复，不是已修复运行时吞吐问题，更不替代全量漏斗与唯一效果瓶颈的选择。

2026-09-10 09:37 补记：续跑后的完整六组结果已从 1,498 增至 1,507，包含此前等待较久的 `persona-912 / row-00734`。随后出现墙钟跳变；电源日志确认本机于 `08:32:56 +0800` 进入 `Clamshell Sleep`，中间仅有短暂 DarkWake，至 `09:33:45 +0800` 才正常唤醒。原评测 PID 7040、session 70054 仍存活，唤醒后同一句柄轮询未返回退出状态，没有因观察中断而启动重复进程。

09:36:10 检查时结果仍为 1,507 题，Episode 总数 59,279，待巩固 Job 为 8、最高 attempts 为 9，待同步索引操作为 55；索引与 Worker window 日志已恢复更新，最近成功模型 usage 日志当时仍停在 08:19:59。当前窗口剩余 57%、周剩余 75%，未显示额度耗尽。不能把进程存活等同于所有后台调用健康；继续按真实终端状态与队列推进判断。本次约一小时的休眠影响运行墙钟与可能的在途请求等待，原始记录保留，不能用于框架速度排名。未修改电源设置、阻止合盖、重启服务或调整冻结实验配置。

2026-09-10 10:47 补记：PID 7040、session 70054 再次明确返回 exit 1，错误为 `effect preparation incomplete: {'episodes': 84, 'jobs': 3, 'pending_jobs': 1, 'pending_index_operations': 0}`；这是原 1,800 秒历史准备期限到期，不是配额错误或新内容拦截。保留 1,535 份完整六组结果。10:43 的只读检查仍见索引 RPC 和窗口加载 deadline，待办中有两个窗口的 attempts 已到 13；没有把等待中的样本记作错答，也没有改写 Job、增大超时或降低并发。

原后台服务继续处理同一批窗口，未完成 Job 从 7 个降到 2 个，再在 10:46:46 检查时降为 0；同一时点待同步索引操作为 0，Episode 总数为 60,406。断点审计通过：1,535 题六组完整性、487 份与数据库相等的冻结快照、483 份完成证明；成功请求缓存为 7,998 份。全量 verifier 仍因缺题拒绝通过，Core 二进制、manifest、恢复器及六份 harness 文件保持原 hash。五个 Core 的 `/health/ready` 均为 200，检查时窗口剩余 94%、周剩余 74%。

确认旧评测已退出、没有另一份同恢复器进程后，于 10:47:24 以全部原参数启动 PID 11548、session 47356，使用同一输出目录和缓存；剩余 517 道可执行缺题，原四个 persona 的 9 题仍暂留。此次仅恢复已经通过断点审计的原实验，后台服务未重启。重复的准备超时说明运行时吞吐问题尚未修复；这份恢复记录不替代全量效果漏斗，也不把改变基线配置当作已完成唯一效果优化。

2026-09-10 11:33 运行背压调整：第三个续跑进程 PID 11548、session 47356 已在 11:19 核验时明确 exit 1，错误为 `effect preparation incomplete: {'episodes': 56, 'jobs': 2, 'pending_jobs': 1, 'pending_index_operations': 0}`。完整六组结果为 1,544 题；此次仍是准备超时，不能据此定位具体 persona，也不是已观测到的额度或内容拦截错误。原后台服务在 11:22:51 前排空巩固和索引队列，Episode 总数为 60,831。随后只读审计通过 1,544 题六组结果、490 份与当前数据库相等的冻结快照、486 份完成证明；成功请求缓存文件共 8,047 份，这一计数不冒充缓存正文逐份审计。全量 verifier 仍因缺题拒绝通过。

在 evaluator 已退出、后台队列为空时，对同一索引进程、同一 collection、同一 owner 的既有查询做只读容量探测：每条请求流串行执行相同的 32 条不同查询，2 条请求流的窗口耗时为 44.764–45.238 秒，10 条为 180.131–187.091 秒。10 流的 320 次 RPC 中有 168 次超过 5 秒，10 个窗口全部超过 120 秒；探测采用 15 秒单 RPC 期限，实际没有 RPC 错误，两组返回的有序候选 hash 全部一致。查询集合 SHA-256 为 `effb14968380f525d4cfd797a9080f282bec139e2d9c1bfc0fe4c43ba6d151c7`。此探测不生成文本、不写 canonical memory，但会增加索引诊断日志；它不是生产并发 Upsert 的精确重放，也不能代表所有 owner 或排除全部主机负载影响。

源码中每个 Core 实例各有一个巩固处理器和索引 projector，共享一个串行编码／Chroma 索引服务。上述证据支持先降低后台竞争，而不是把数据库加载 deadline 单独认定为慢 SQL。于 11:33:10 核对五个 Core 的 PID、父 PID、启动时间、工作目录及相同二进制 hash 后，只对额外四个副本 `69487 / 69496 / 69503 / 69510` 发送可恢复的 `SIGSTOP`，逐一验证为 `T`；保留原始 Core `68017`、Worker `67576`、索引 `67944` 和 MySQL 运行，主实例 `/health/ready` 返回 200。11:41 的再次只读核对仍符合该状态。未操作 Chorai 主线、Omni 服务或权威数据库记录。

这是在第 1,544 题断点把后台拓扑从 5 实例调回初始 1 实例，已独立记录于 `runtime-replicas.json`。8 persona 并发、六组、模型、prompt、预算、超时、重试、算法、Core/Worker/index 版本和原 manifest 不变；不能把“后台拓扑也完全未变”写入最终报告。该调整不计作已完成唯一效果瓶颈优化，也尚未证明解决持续生产负载的全部超时；后续需要实际新增 checkpoint 验证。保留所有原始耗时并披露此边界，不做速度排名。恢复器预检确认仍有 508 道可执行缺题，原四个 persona 的 9 题继续暂留。

11:44:20 已以全部原 harness 参数启动新的 evaluator PID 14298、session 47721。启动前再次逐份核验 1,544 题结果、490 份数据库冻结快照、486 份完成证明，通过；恢复器、manifest 和 Core 二进制 hash 保持原值，主 Core 就绪检查为 200。日志确认原四个暂留 persona 均为 `no_request`。11:45:50 检查时新进程存活，Episode 已增至 60,883，巩固／索引开始出现新待办，完整题目数尚未增加；这是实际恢复了历史准备，不是新题已完成或全量效果已通过的证据。

11:54 续跑核验：同一 PID 14298／session 47721 持续存活，已从 1,544 增至 1,553 份完整六组结果，新增 9 题，暂留名单未扩大。11:54:40 查询为 61,276 个 Episode、7 个待巩固 Job（当前集合最高 attempts=1）、26 个待同步索引操作；Worker usage/window 与索引日志均在持续更新。

仅采集 11:49:30 至 11:54:40 新追加的索引日志（起始字节偏移 `1277532052`），得到 226 次完成的 Search 和 202 次完成的 Upsert：Search p50/p95 为 760.837/2,675.193 ms，Upsert 为 747.441/2,473.026 ms；两类均没有完成耗时超过 5 秒的记录，JSON 解析无残缺行。日志耗时不包含 handler 前排队，且只覆盖已完成调用，因此不能据此证明不存在客户端 deadline 或宣称全部吞吐问题已修复。当前主 Core 终端读到的最后一条 retry 时间仍为调整前的 11:19:22。该短期观察支持继续单实例运行，不能替代全量效果实验或用于跨拓扑速度排名。

12:46 单实例一小时核对：原 evaluator PID 14298、session 47721 自 11:44:20 连续运行，完整六组结果已从 1,544 增至 1,592，新增 48 题，期间未因准备超时退出。12:45:18 的数据库查询为 62,874 个 Episode、8 个待巩固 Job（当前集合最高 attempts=1）、23 个待同步索引操作；Worker 与索引日志持续更新。本次读取主 Core 终端没有新增输出，主实例 `/health/ready` 返回 200，四个额外副本仍为预期的 `T` 状态。这个观察支持继续当前单实例配置，但不把缺少终端错误日志等同于所有客户端调用均无 deadline。

恢复器只读预检通过，仍保留完整 2,061 题，确认 1,592 题已完成、460 题可继续执行、原四个 persona 的 9 题暂留，计分规则未变。恢复器、manifest、Core 二进制及预检覆盖的六份冻结 harness 源码保持原 hash。此处没有重新声称完成全部数据库快照审计。套餐查询正常，窗口剩余 77%、周剩余 72%。这次一小时运行证据已补入 `runtime-replicas.json`，未更改模型、prompt、算法、学习数据或原实验分母；全量基线、最终漏斗与唯一效果瓶颈优化仍未完成。

15:45 单实例四小时核对：原 evaluator PID 14298、session 47721 已连续运行超过四小时，完整六组结果从恢复时的 1,544 增至 1,724，新增 180 题；没有新的 evaluator 终止或暂挂。主实例 `/health/ready` 返回 200，四个额外 Core 副本仍为预期的暂停状态，各进程启动时间均与此前核对值一致。二进制、恢复器和 manifest 的 hash 及冻结预检通过，仍有 328 题可继续执行、原 9 题暂留。15:45:18 的后台状态为 68,802 个 Episode、7 个待巩固 Job（当前最高 attempts=1）、9 个待同步索引操作；套餐查询为窗口剩余 89%、周剩余 69%，状态正常。

本次读取主 Core 终端新增三条 `MemoryIndex projection retry / DeadlineExceeded` 日志，发生于 14:53:32、15:07:45、15:08:31；此前 12:46 的“无新增终端输出”只是当时的观察，并不延伸为四小时完全无错误。15:38:32 的全局索引待办检查已为 0，随后结果继续增长。代码路径核对显示投影应用失败会安排重试而不确认完成，后续循环继续处理；本次不猜测三个单次超时的具体触发原因，也不据此更改实验超时、算法或拓扑。四小时观察已补入 `runtime-replicas.json`，仍不代表全部吞吐问题或唯一效果瓶颈已经解决。

2026-09-10 18:45–18:47 七小时复核：evaluator 仍为 11:44:20 启动的 PID 14298／session 47721，主 Core、Worker、Index 的进程身份与目录未变，额外四个 Core 仍为暂停状态；主 Core `/health/ready` 返回 200。二进制、恢复器与 manifest hash 保持冻结值，恢复器预检通过，原 9 道暂留题未扩大，计分未变；18:46:17 已完成 1,821 道六组结果。主 Core 终端自上次读取后新增四条投影 `DeadlineExceeded` 重试，时间为 16:12:44、18:24:13、18:24:18、18:24:23，不能写成无错误续跑。18:47:31 的全数据库只读查询显示未确认索引操作为 0，没有遗留旧待办；同轮 validation 已有 69,885 条 Episode、6 个待巩固 Job，当前最高 attempts=1。MySQL 队列代码确认重试不提前确认操作，成功确认会清空 `last_error`，因此不凭当前错误字段反推四次单独超时的完整历史或根因。本次仅核查并记录现有恢复行为，未修改代码、超时、拓扑或实验参数，也不将其计作唯一效果瓶颈优化。

2026-09-10 20:52 休眠后复核：主机电源日志确认 19:43:22 进入 `Clamshell Sleep`，19:44:18 还记录过 `Thermal Emergency Sleep`，随后反复维护休眠和短暂 `DarkWake`，直到 20:43:54 才恢复完整 `Wake`。因此 evaluator 的进程存活时长不等于持续执行时长，这个区间的整体壁钟耗时不能用作模型或 Core 的速度比较。当前 `pmset -g therm` 未提供热警告等级、性能警告等级或 CPU 功耗状态，不能据此断言当前温度正常或判断硬件故障；未修改电源或保护设置。原 evaluator PID 14298／session 47721、主 Core、Worker、Index 和暂停的四个额外 Core 均保持原身份，主 Core 就绪接口为 200，冻结 hash 与恢复器预检通过，暂留仍为原 9 题，计分未变；20:52:07 已完成 1,864 题。六组 `duration_ms` 使用 `perf_counter` 围绕模型接口调用及评分记录，且调用可能命中请求缓存；本次保留全部原始值，不推断每条记录包含多少系统休眠时间、不统一扣减、不重跑已有答案，也不发布本轮速度排名。

2026-09-10 22:54–22:55 重试恢复复核：本次补读主 Core 终端，看到休眠区间的 19:44:10 投影 deadline，以及 19:44:18、19:45:36 两次巩固 `APIConnectionError`；这些与已记录的休眠时间重叠，但不能据此确定每次 RPC 的完整根因。另有 22:50:48 巩固 deadline 和 22:51:56 投影 deadline。只读任务查询确认最新二次尝试任务 `consolidation-job-4d3e6e120d99c838ec613079b4531966`（persona-745，31 个 Episode）于 22:52:44 完成，22:54:28 待办集合最高 attempts 已恢复为 1；终端错误没有 job_ref，不能把时间对应冒充精确调用归因。22:55:12 查询确认本轮 tenant 在 22:51:56 及以前创建的未确认索引操作为 0，新待办仍可继续产生。原 evaluator PID 14298／session 47721 继续运行，未重启服务、修改超时或调整后台拓扑；本次只记录既有自动重试完成，不宣称消除了全部超时。

## 可执行样本完成与剩余缺口

2026-09-11 09:15 核验：原 PID 14298 已不存在，原 session 47721 明确返回 exit 1；最终异常来自 `verified_effect_summary` 的 `results must cover exactly the frozen validation questions`，不是新的额度错误或准备超时。恢复器只读预检为 2,052 题已完成、0 题可继续执行、原四个 persona 的 9 题暂缓，评分未变。validation tenant 已有完整 84,290 条 Episode，待巩固 Job 和待确认索引操作均为 0；未因观察中断或进程退出而重新启动评测。

结束后只读审计通过：2,052 题六组结构与身份检查，735 份冻结快照逐份与当前数据库相等且对应 owner 的待同步索引为 0，731 份完成证明通过快照 hash、完整题目集合与记忆未变声明检查。成功请求缓存共 10,739 份；这一计数不冒充全部缓存正文审计。全量 verifier 仍拒绝缺题结果，未发布全量成绩或选择效果瓶颈。

精确缓存恢复检查不调用模型、不写文件或 canonical memory：按冻结模型版本、prompt、选项顺序、`PYTHONHASHSEED=0` 和输出预算重新构造 `none / full_history / oracle_episode` 请求，先以 3 道已完成题的 9 个缓存请求作阳性校验，全部命中。9 道暂缓题中有 8 道至少缺少上述一个必需请求；余下 `persona 3 / row-00364` 再以同一 owner、原 query 和冻结 Episode 文本做一次只读 MemoryIndex 查询，返回完整 40 个 Episode，但对应 `semantic_top40` 请求缓存不存在。该题匹配 prompt 与选项的成功缓存仍仅有 3 份；缺失语义请求 SHA-256 为 `0514e422b214ccdc33d46d95a3e87bc56b783c59a2bcb7531477da41ad14cfee`。因此当前缓存不足以恢复任何一道暂缓题的完整六组结果。

剩余不是“9 次已确认拦截”：persona `3 / 335 / 756` 的 8 题受一次未精确归因的 422 与并发取消影响，persona `286 / row-01756` 是已保存真实请求的另一次 422。不得把未执行请求全部标为供应商拦截，也不得伪造答案、删题或缩小分母。需要明确失败计分与取消请求补跑口径后再推进；本次没有改 Core、Worker、索引算法、答案 prompt 或恢复器，不计作唯一效果瓶颈优化。

## 本次无 Key 验证

- 2026-09-11 第 1,977 题断点只读复核通过：捕获的 1,977 道完整六组结果通过结构与身份检查，683 份冻结快照与当前数据库一致且对应 owner 的待同步索引为 0，678 份完成凭证通过快照 hash、完整题目集合和记忆未变声明检查。成功请求缓存文件计数为 10,351，未逐份核验缓存正文；全量 verifier 仍以 `results must cover exactly the frozen validation questions` 拒绝缺题结果。沿用先捕获完成凭证、再捕获题目和快照的一致顺序，不调用模型、不写 canonical memory，也不修改实验条件或计分；本次通过不代表全量基线完成，不能据此提前选择或修复效果瓶颈。

- 2026-09-10 第 1,918 题断点只读复核通过：捕获的 1,918 道完整六组结果通过结构与身份检查，651 份冻结快照与当前数据库一致且对应 owner 的待同步索引为 0，647 份完成凭证通过快照 hash、完整题目集合和记忆未变声明检查。成功请求缓存文件计数为 10,043，未逐份核验缓存正文；全量 verifier 仍以 `results must cover exactly the frozen validation questions` 拒绝缺题结果。继续采用一致捕获顺序，不调用模型、不写 canonical memory，也不改变冻结实验或计分；这些计数只代表本次捕获集合，不代表完整 validation 已完成。

- 2026-09-10 20:52 休眠后只读断点复核通过：捕获的 1,863 道六组结果通过结构与身份检查，623 份冻结快照与当前数据库一致且对应 owner 的待同步索引为 0，618 份完成凭证通过快照 hash、完整题目集合和记忆未变声明检查。成功请求缓存文件计数为 9,745，未逐份核验缓存正文；全量 verifier 仍以 `results must cover exactly the frozen validation questions` 拒绝缺题结果。采用既有一致捕获顺序，检查不调用模型、不写 canonical memory，不改变冻结实验或计分；上述数字仅代表本次捕获集合。

- 2026-09-10 18:12 只读断点复核通过：捕获的 1,800 道六组结果通过结构与身份检查；597 份冻结快照与当前数据库一致，对应 owner 的待同步索引为 0；592 份完成凭证通过快照 hash、完整题目集合和记忆未变声明检查。继续采用先捕获完成凭证、再捕获题目与快照的顺序，避免并行续跑造成集合时间差。成功请求缓存共计 9,410 份，仅做文件计数；全量 verifier 仍以 `results must cover exactly the frozen validation questions` 拒绝缺题结果。原 evaluator PID 14298／session 47721 继续运行；检查不调用模型、不写 canonical memory，也不修改冻结实验或计分。
- 2026-09-10 进度计数口径复核：固定 validation 的 2,061 题对应 735 份历史，经现有解析器得到 84,290 个完整 Episode，每人 75–167 个；先前预检中的 23,143 个 Episode 属于 Omni 的 200 人 benchmark，不能作为 validation 分母。数据库分 tenant 查询另有 7 个旧 smoke scope，合计 4,039 个 Episode；此前后台绝对计数是含 smoke 的数据库总量。16:50 的只读查询中，本轮 `personamem-personamem-effect-minilm-validation-all-v1` 为 66,568 个 Episode。后续 Episode 与待办进度均按这个 tenant 过滤，历史记录中的总量不回写成新口径。MemoryIndex 的 Search 仍使用 tenant、agent、relationship 的联合精确过滤；此次只澄清进度统计，未改学习数据、检索、manifest 或计分。
- 2026-09-10 投影重试边界复核：`go test ./memoryindex -run '^TestProjector' -count=1` 通过。既有用例覆盖投影失败安排重试且不提前确认、重试成功后确认等行为；它验证的是失败处理契约，不证明本次三个真实超时的触发原因或生产吞吐已修复。本次未修改 Core 代码。
- 2026-09-10 15:09 再次只读断点复核完成：捕获的 1,695 道六组产物通过结构与身份检查，550 份冻结记忆快照与数据库一致且对应 owner 的待同步索引为 0；545 份完成凭证通过快照 hash、完整题目集合和记忆未变声明检查。此次先固定已存在的完成凭证集合，再捕获题目与快照路径，避免运行期间新凭证加入造成检查集合的时间差。成功请求缓存文件计数为 8,851，未逐份核验正文；全量 verifier 仍以 `results must cover exactly the frozen validation questions` 拒绝缺题结果。检查不调用模型、不写 canonical memory，不改变冻结实验或计分；上述数字仅代表本次捕获集合。
- 2026-09-10 续跑中只读断点审计：本次捕获的 1,632 道完整六组结果通过身份、Oracle 隔离与漏斗检查；523 份冻结记忆快照逐份与当时的数据库状态相等，对应 owner 的待同步索引为 0；519 份 persona 完成证明的快照 hash、完整题目集合和记忆未变声明通过。成功请求缓存文件数为 8,520，仅是文件计数，不声称本次逐份核验了缓存正文。全量 verifier 仍明确以 `results must cover exactly the frozen validation questions` 拒绝通过。审计不发送模型请求、不写 canonical memory；这些计数是审计捕获的集合，不冒充运行中不断增长的实时总数。
- 2026-09-10 增补投递边界核对：MySQL 路径从 canonical Source 正文构造 Episode Evidence，Core 字节预算和 Python SDK 总 token 预算均只保留完整条目，超预算整条跳过；SDK 只为真正纳入渲染正文的条目登记 `memory_refs`，六组 Runner 将该正文完整加入模型 instructions。所检查路径不存在“预算截断 Episode 后仍仅凭引用 ID 计作完整投递”的情况。重新运行 SDK Episode rendering、六组 Context 与漏斗定向测试：29 passed；Go `TestBoundEpisodeEvidence|TestEpisodeEvidenceBudget` 定向测试通过。未改变代码、prompt、计分、历史数据或实验参数；该边界核验不代表全量效果已通过。
- `make verify-eval` 成功：Python 186 passed、2 skipped；TypeScript 类型检查、3 项测试与构建通过，生成协议比对及 wheel 构建通过。
- 两项跳过是隔离测试环境未安装 Chroma 的真实索引用例。使用现有系统 Python、Chroma 和固定 MiniLM 缓存，在临时数据库中补跑 `test_local_memory_index.py`：5 passed，包含这两项真实索引用例；没有修改正在评测的数据库或 Python 依赖。
- `git diff --check` 通过。上述验证证明工具与断点机制可用，不代表模型效果或全量基线已经通过。

## 尚未完成

两套全量回答、最终计分、完整漏斗与唯一最大瓶颈的工程改动及验证仍未完成。只有两套基线完成后，才按既定规则选择并修复一个有证据支持的瓶颈；本记录不代替最终效果报告。
